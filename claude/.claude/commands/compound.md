---
description: Document a recently solved problem into the LLM Wiki (docs/ detail + learnings/ synthesis)
argument-hint: "[optional: brief context] [mode:headless]"
---

Invoke the `compound` skill to document the most recently solved problem.

Pass `$ARGUMENTS` through as the skill's argument string (brief context hint and/or `mode:headless`). Follow the skill's workflow exactly; do not duplicate its logic here.
